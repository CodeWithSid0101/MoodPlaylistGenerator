{
  "name": "mood-playlist-generator",
  "version": "1.0.0",
  "description": "A web application that generates music playlists based on user's mood",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install && cd client && npm install && npm run build",
    "client": "cd client && npm start",
    "server": "nodemon server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "axios": "^1.4.0",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "keywords": [
    "music",
    "playlist",
    "mood",
    "spotify",
    "react",
    "node"
  ],
  "author": "CodeWithSid0101",
  "license": "MIT"
}
